#![allow(dead_code, unused)]

fn main() {
    // Numeric operations, allow in integer and float data types
    // addition
    let sum = 5 + 10;

    // subtraction
    let sub = 33.4 - 22.2;

    // multliplication
    let mult = 4 * 5;

    // devision
    let quotient = 5.0 / 2.0;
    println!("{quotient}"); // 2.5
    // truncated division
    let truncated = 5 / 2;
    println!("{truncated}"); // 2

    // remainder
    let remainder = 43 % 5;
    println!("{remainder}"); // 3
}
